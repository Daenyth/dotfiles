#!/bin/bash
if [[ -z $SSH_AGENT_PID ]]; then
    eval `ssh-agent`
fi
screen -t irc
screen -t music
sleep 1
screen -t shell bash --rcfile /usr/bin/git-sh
screen -t web
sleep 1
screen -t IM
screen -t shell
sleep 1
screen -t torrent
screen -t X
